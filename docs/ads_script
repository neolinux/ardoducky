write <str>:
    Writes an ASCII string

wait <time>:
    Waits <time> milliseconds

press <key>[+key...]:
    Presses and releases a keycombination

:<label>:
    Defines a label

jump <label>:
    Jumps to a label

writeraw <str>:
    Writes ASCII string without using ALT-key. Useful for systems other than
    Windows but causes incompatibility with keyboard layouts and behaviour
    is unknown for many characters.
